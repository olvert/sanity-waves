version: '3.5'

services:
  next-waves:
    image: olvert/next-waves
    environment:
      - VIRTUAL_HOST=dev.waves.olle.io
    networks:
      - proxy-net

networks:
  proxy-net:
    external: 
      name: nginx-proxy
